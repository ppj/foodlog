<h3>Welcome, <%= link_to @user.firstname, user_path(@user) %>!</h3>

<p>
  <% if @user.meals.any? %>
    Your Meals:
  <% end %>
  <ul>
    <% @user.meals.each do |meal| %>
      <li><%= meal.category %>: <%= meal.description %></li>
    <% end %>
  </ul>
</p>
